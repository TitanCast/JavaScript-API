var ConnectionStates = {
    NOT_CONNECTED: "not connected",
    CONNECTED: "connected",
    AWAITING_RESPONSE: "awaiting response"
};

var AccelerometerSpeed = {
    GAME: "game",
    FASTEST: "fastest",
    UI: "ui"
}
